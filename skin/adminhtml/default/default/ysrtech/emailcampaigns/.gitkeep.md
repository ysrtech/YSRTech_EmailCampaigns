# Built editor bundles are committed here so target installs need no Node.
# Rebuild with: cd frontend && npm install && npm run build
# editor.js / editor.css will appear in this directory after a build.
